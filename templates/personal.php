<?php

  // Not needed at the moment
  // script('paperwork', 'settingsview');
  // script('paperwork', 'settings');
  // style('paperwork', 'settings');

?>

<!-- <div class="section">
  <h2><?php p($l->t('Money')); ?></h2>
  <div id="money-settings">
    <label><?php p($l->t('Account summary currency')); ?>:</label>
    <input type="text" placeholder="<?php p($l->t('Account summary currency')); ?>"><?php echo $accountSummaryCurrency; ?></input>
    <ui-select title="Choose a currency" theme="select2" tagging tagging-label="false">
      <ui-select-match placeholder="{{ ctrl.t.placeholderCurrency }}">{{$select.selected}}</ui-select-match>
      <ui-select-choices repeat="currency in ctrl.availableCurrencies | filter: $select.search">
        <span ng-bind-html="currency | highlight: $select.search"></span>
      </ui-select-choices>
    </ui-select>
  </div>
</div> -->
